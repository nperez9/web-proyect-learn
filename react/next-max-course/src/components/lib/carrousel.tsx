import Image from 'next/image';

export interface CarruselProps {}

const Carrusel = () => {
  return <div id="default-carousel" className="relative w-full h-full" data-carousel="slide"></div>;
};

export default Carrusel;

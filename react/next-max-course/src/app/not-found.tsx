import { Messages } from '@/components/lib/messages';

// 404 page
export const NotFound = () => {
  return <Messages title="Page not found" subtitle="Probably the link is wong" />;
};

export default NotFound;
